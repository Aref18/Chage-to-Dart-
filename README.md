# Chage-to-Dart-

## 📌 Project Language Notice

This repository is developed primarily using Dart and the Flutter framework.

Some platform-specific folders such as `windows/`, `linux/`, `macos/`, `android/`, and `ios/` contain automatically generated native runner code (C/C++ and other platform code). These files are created by Flutter to enable multi-platform support and do not represent the core logic of the project.

To ensure accurate language statistics on GitHub, generated platform files are excluded using a `.gitattributes` configuration file.

The main application logic, UI implementation, and architectural structure are fully written in Dart.

Framework: Flutter  
Language: Dart  
Purpose: Educational / Training Project

--------------------------------------------------------------------------------------------------------------------

## 📌 توضیح درباره زبان پروژه

این ریپازیتوری به صورت کامل با زبان Dart و فریم‌ورک Flutter توسعه داده شده است.

پوشه‌هایی مانند `windows/`، `linux/`، `macos/`، `android/` و `ios/` شامل کدهای Native تولیدشده توسط Flutter هستند (مانند C/C++). این فایل‌ها به صورت خودکار برای پشتیبانی چندسکویی ایجاد می‌شوند و منطق اصلی برنامه محسوب نمی‌شوند.

برای اینکه آمار زبان در GitHub به درستی نمایش داده شود، فایل‌های تولیدشده از طریق تنظیمات `.gitattributes` از محاسبه زبان حذف شده‌اند.

منطق اصلی برنامه، رابط کاربری و ساختار معماری پروژه به طور کامل با Dart پیاده‌سازی شده است.

فریم‌ورک: Flutter  
زبان اصلی: Dart  
هدف پروژه: آموزشی
